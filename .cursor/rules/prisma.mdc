---
description: Prisma, схема и repositories
globs: prisma/**,src/repositories/**,src/lib/prisma.ts
alwaysApply: false
---

- PostgreSQL, изменения схемы — только через Prisma migrations
- Prisma-запросы — только в `src/repositories/`, не в services и не в API

# Доменные модели

**Purchase:** date, amountUsdt, btcPrice, btcAmount, source (`manual` | `dca`), notes
